package com.aiassistant.data.daos;

import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.OnConflictStrategy;
import androidx.room.Query;
import androidx.room.Update;

import java.util.List;

/**
 * DAO for ActionSuggestion
 */
@Dao
public interface ActionSuggestionDao {
    
    // This is a placeholder interface that will be implemented later
    // when we have the ActionSuggestion model defined
}
